#include<stdio>
/**
 * main -main block
 * Return: 0
 */
int main(void)
{
	if (n > 0)
	{
		printf("is positive");
	}

	else if (n == 0)
	{
		printf("is zero");
	}

	else if (n < 0)
	{
		printf("is negative");
	}
	return (0);
}
